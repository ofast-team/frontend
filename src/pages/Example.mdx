import { Box } from '@mui/material'
import Example2 from './Example2.mdx'
import MCQBlock from '../components/MCQBlock'

export function Thing() {
  return <span>World!</span>
}

<Box sx={{height: '300px', width: '500px', marginTop: '100px', bgcolor: 'green'}}>
  Test Green Box MUI
</Box>

# Hello <Thing/>

$a^2 + b^2 = c^2$

$$a$$

```python
# Create a dictionary to store previously computed Fibonacci values
memo = {}

def fibonacci(n):
    # Check if the value is already memoized
    if n in memo:
        return memo[n]

    # Base cases
    if n <= 1:
        return n

    # Calculate and memoize the Fibonacci value
    memo[n] = fibonacci(n - 1) + fibonacci(n - 2)
    return memo[n]

# Example usage:
n = 10
result = fibonacci(n)
print(f"The {n}-th Fibonacci number is {result}")
```

<MCQBlock question={<Example2/>} answerOptions={[]} correctOptions={[]} hint="" explanation=""/>
import MCQBlock from '../../../components/MCQBlock'

<MCQBlock 
  question="In the previous example, what would be the base case of the recursive function?"
  answerOptions={["F(0) = 1", "F(0) = 2", "F(n) = 2 * F(n)", "F(n) = 2 * F(n) - 1"]}
  correctOptions={["F(0) = 2"]}
  hint="DP is used to solve and store subproblem."
  explanation="DP = AC"
  />

import MCQBlock from '../../components/MCQBlock'

<MCQBlock 
  question="In dynamic programming, what is the key characteristic that distinguishes it from other algorithmic approaches?"
  answerOptions={["Recursion", "Iteration", "Memoization"]}
  correctOptions={["Memoization"]}
  hint="Store and reuse results to avoid redundant computations."
  explanation="Memoization caches expensive functions calls and reuses when same input occurs."
  />
### Quiz: Time complexity
<MCQBlock question = "What is the time complexity of computing the prefix sums for an array of size $n$?"
answerOptions ={[
'$O(n^2)$',
'$O(1)$',
'$O(n)$',
'$O(log(n))$']}
optionVerdicts = {[false, false, true, false]}
hint = "Prefix sum array loops through array and stores sum for each index `i`."
explanation= "Computation of the prefix sum array is linear runtime $O(n)$, as it loops through the entire array and stores cumulative sum for each index `i`."
/>
### Quiz: Prefix Sum array
<MCQBlock 
    question = "What is the __prefix sum array__ of the array $arr = [2, 5, 3, 1, 8, 4, 7, 6]$ and the __range sum__ for indices $[2, 6]$?"
    answerOptions ={[
    '$prefix = [2, 5, 3, 1, 8, 4, 7, 6]$',
    '$range [2, 6] = 20$',
    '$prefix = [2, 7, 10, 11, 19, 23, 30, 36]$',
    '$range [2, 6] = 23$'
    ]}
    optionVerdicts = {[false, false, true, true]}
    hint = "Use the formulas from the lesson to calculate the prefix array and range sum query accordingly."
    explanation= "Computed `prefix` array is using `prefix[i] = prefix[i-1] + arr[i]` with `prefix[0] = arr[0]` and to calculate the range sum for $[2, 6]$ is `prefix[6] - prefix[2-1]`."
/>
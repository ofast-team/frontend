# Input/Output Practice
<FITBBlock hint="Think about the direction information flows when using `cin` and `cout` respectively.">
    Let's say that you want to input to a variable `x` and print it out in C++. Fill in the correct arrows to do this.

    `cin` <FITBBlank correctAnswer=">>"/> `x;`

    `cout` <FITBBlank correctAnswer="<<"/> x <FITBBlank correctAnswer="<<"/> `endl;`
</FITBBlock>
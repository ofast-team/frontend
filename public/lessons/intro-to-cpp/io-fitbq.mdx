# Input/Output Practice
<FITBBlock hint="Think about the way that the variables go into `cin` and `cout`">
    Lets say that you want to take in a variable `x` and print it out in C++. Fill in the correct arrows to do this.

    `cin` <FITBBlank correctAnswer=">>"/> `x;`

    `cout` <FITBBlank correctAnswer="<<"/> x <FITBBlank correctAnswer="<<"/> `endl;`
</FITBBlock>
<MCQBlock
  question="In the previous example, what would be the base case of the recursive function? (test latex: $a^2$)"
  answerOptions={[
    '$F(0) = 1$',
    '$F(0) = 2$',
    '$F(n) = 2 * F(n)$',
    '$F(n) = 2 * F(n) - 1$',
  ]}
  optionVerdicts={[false, true, false, false]}
  hint="DP is used to solve and store subproblem. (test latex: $a^2$)"
  explanation="DP = AC (test latex: $a^2$)"
/>
